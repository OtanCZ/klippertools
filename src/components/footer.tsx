import Image from 'next/image'

export default function Footer() {
    return (
        <footer className={"flex flex-row bg-secondary content-center justify-center w-screen border-t-2 border-accent"}>
            <div className={"flex flex-col justify-center content-center"}>
dsadsad
            </div>
        </footer>
    )
}
